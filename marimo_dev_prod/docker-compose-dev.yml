services:
  marimo:
    build:
      context: ./marimo-image
      args:
        PYTHON_VERSION: ${PYTHON_VERSION}
    container_name: marimo-dev
    command: marimo edit --no-token --host 0.0.0.0 -p 8080
    env_file:
      - .env
    volumes:
      - ./marimo-image/scripts:/app/scripts
    working_dir: /app/scripts
    ports:
      - "${MARIMO_PORT}:8080"
    stdin_open: true
    tty: true
